<h1>Playlists</h1>

<div id="playlists">
    <% @playlists.each do |playlist| %>
        <%= render playlist %>
        <p>
            <%= link_to "Show this playlist", playlist %>
        </p>
    <% end %>
</div>

<%= link_to "New Playlist", new_playlist_path %>
